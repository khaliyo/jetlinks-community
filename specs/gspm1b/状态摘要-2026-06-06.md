# GSPM1B 接入状态摘要

> **日期：** 2026-06-06  
> **完整上下文：** [../状态摘要-2026-06-06.md](../状态摘要-2026-06-06.md)  
> **前序：** [状态摘要-2026-06-05.md](./状态摘要-2026-06-05.md)

---

## 1. 一句话结论

GSPM1B 已稳定走 **`gemeopen-mqtt` 协议 JAR**（非透传）；运行状态页 **history + WS realTime** 经重登后正常；代码在私有 fork **`khaliyo/jetlinks-community`** 分支 **`feature/gemeopen-gspm1b`**（`f63194fa`）。

---

## 2. 设备侧状态

| 项 | 状态 |
|----|------|
| 协议 | `gemeopen-mqtt` |
| 上行 Topic | `/gspm1b/{deviceId}/up` |
| 下行 Topic | `/gspm1b/{deviceId}/down` |
| 透传脚本 | 已删除 |
| 属性 / 功能 / 实时 UI | ✅ |

---

## 3. 运行状态 WS（本会话结论）

- 页面逻辑未改：`Property/index.vue` → POST `history` + WS `properties/realTime`  
- **仅 ping/pong**：WS 已连但未 `sub` 或 **token 过期**  
- **重启后需重登**：`/messaging/{token}` 路径含 token，与 HTTP 会话不同步时的常见现象  
- 重登后见 `WebSocket connected` + 设备上报时有 `result` 即正常  

---

## 4. 仓库

- 不向 JetLinks 官方提 PR  
- 推送：`git push origin feature/gemeopen-gspm1b`  
- `specs/` 暂未提交 git  

---

*2026-06-06 会话归档。*
